# MongoDB URI for local development or cloud (MongoDB Atlas if using it)
MONGO_URI=mongodb://localhost/my-demo-website   # Change this to your MongoDB connection string if using Atlas

# Port on which your server will run
PORT=5000

# JWT Secret for signing JSON Web Tokens (important for authentication)
JWT_SECRET=your_jwt_secret_here  # Replace with a secure secret key

# Set the environment (local, production, etc.)
NODE_ENV=development   # Change this to "production" when you deploy to production

# Optional: API keys or third-party services (if applicable)
# Example:
# SENDGRID_API_KEY=your_sendgrid_api_key_here
